# Backend (Nest) URL. Exposta ao navegador, dai o prefixo NEXT_PUBLIC_.
# ATENCAO: NEXT_PUBLIC_* e inlinada no BUILD, nao lida em runtime — mudar isso
# exige rebuild do web.
NEXT_PUBLIC_API_URL="http://localhost:5000"
