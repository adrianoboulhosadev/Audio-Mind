// The summary context takes no request payload of its own: it is written by the
// pipeline and read by recording id. Kept as a file (empty of inputs) so the
// package layout stays identical across contexts.
export {}
